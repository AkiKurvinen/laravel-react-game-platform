import { NavLink } from "./NavLink";

const meta = {
    title: "Design System/Atoms/NavLink",
    component: NavLink,
    tags: ["autodocs"],
};

export default meta;

export const Default = {
    args: {
        target: "#body",
        label: "Link",
    },
};